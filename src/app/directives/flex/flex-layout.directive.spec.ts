import { FlexLayoutDirective } from './flex-layout.directive';

describe('FlexLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
